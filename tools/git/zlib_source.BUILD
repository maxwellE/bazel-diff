# Overlay BUILD file for the @zlib_source repository (zlib release tarball),
# consumed by the rules_foreign_cc build in //tools/git:zlib.

filegroup(
    name = "all_srcs",
    srcs = glob(["**"]),
    visibility = ["//visibility:public"],
)
